name: Test Node application
on:
  workflow_call:
    inputs:
      working-directory:
        type: string
        required: true

jobs:
  build:
    name: Build ${{ inputs.working-directory }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Setup node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: ${{ inputs.working-directory }}/package-lock.json
      - name: Install node.js dependencies
        run: npm ci
      - name: Build application without SSG
        run: npm run build-skip-ssg
        env:
            ENONIC_API: "needed for build, but is not used when skipping SSG"
      - name: Run unit tests
        run: npm run test
